let valores =[5,8,2,9,3]
console.log(valores)
/*num.sort() //ordena o vetor
num.push(1) // coloca o número na ultima posição do vetor
num.length // mostra o tamanho do vetor
console.log(num)

for(let pos = 0; pos < num.length; pos++){
    console.log(num[pos])
}
for(let pos in valores){ //para cada pos dentro em valores
    console.log(`A posição ${pos} tem o valor ${valores[pos]}`)

}*/
let pos = valores.indexOf(6)
console.log(pos)

